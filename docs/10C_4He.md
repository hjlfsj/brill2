# 10C+4He — 10C+4He 符合测量与 14O 激发能重建

## 功能概述

10C+4He 符合测量分析，用于研究 14O 的激发态。程序分为两个阶段：

- **extract_10C_4He**：从 match/beam/track/ingot 等原始数据中提取 10C+4He 符合事件，经筛选和能量刻度后输出自包含的 ROOT 文件
- **GUI_10C_4He**：交互式 GUI，读取 extract 阶段的输出文件，进行可视化筛选和物理分析，包括 14O 激发能谱重建

---

## 程序结构

| 路径 | 用途 |
|------|------|
| `src/brill/bin/10C+4He/extract_10C_4He.cpp` | 数据提取主程序 |
| `src/brill/bin/GUI_10C_4He.cpp` | 交互式可视化主程序 |
| `src/brill/include/10C+4He/10C_4He_event.h` | C10He4Event 结构体定义 |
| `src/brill/src/10C+4He/10C_4He_event.cpp` | SetupInputC10He4 / SetupOutputC10He4 实现 |
| `src/brill/include/10C+4He/extract.h` | C10He4Calibration 结构体、刻度函数、筛选函数声明 |
| `src/brill/src/10C+4He/extract.cpp` | ReadC10He4Calibration / Pass10C_d3_4He_s1Cut 实现 |
| `src/brill/src/10C+4He/CMakeLists.txt` | C10He4_event 和 C10He4_extract 库构建 |
| `src/brill/include/rebuild/nuclear_data.h` | GetMass 函数，从 amdc_ion_2020.txt 获取原子质量 |
| `src/brill/include/physics/kinematics.h` | AngleBetween / AngleWithZ 角度计算函数 |
| `src/brill/Cut/cal_d2_d3_stop_10C_cut.C` | TCutG 截断，筛选 (E3_10C, E2_10C) 区域 |
| `src/brill/Cut/cal_d4_s1_stop_4He_cut.C` | TCutG 截断，筛选 (E5_4He, E4_4He) 区域 |

---

## 探测器布局与能量定义

10C+4He 符合事件中，10C 和 4He 两个碎片击中不同的探测器层：

| 探测器 | 碎片 | 能量变量 | 说明 |
|--------|------|----------|------|
| T0 D1 | 10C + 4He | e1_10C, e1_4He | 第一层 DSSD，两个 hit 同时击中 |
| T0 D2 | 10C + 4He | e2_10C, e2_4He | 第二层 DSSD，两个 hit |
| T0 D3 | 10C + 4He | e3_10C, e3_4He | 第三层 DSSD，两个 hit |
| T0 D4 | 4He | e4_4He | 第四层 DSSD，4He 停止 |
| T0 S1 | 4He | e5_4He | CsI 闪烁体，4He 残余能量 |

10C 总动能：T_10C = e1_10C + e2_10C + e3_10C
4He 总动能：T_4He = e1_4He + e2_4He + e3_4He + e4_4He + e5_4He

---

## extract_10C_4He — 数据提取

### 运行方式

| 参数 | 说明 |
|------|------|
| `-r, --run` | 起始 run 号（必填） |
| `-e, --end-run` | 结束 run 号（可选，默认等于起始 run） |
| `-t, --trigger` | 触发类型：`main` 或 `t1`（必填） |
| `-c, --config` | 配置文件路径（默认 `config.toml`） |
| `--d1_hit` | D1 上 hit 数：`1`=仅 10C（4He 在 D1 丢失），`2`=两者都有（默认） |
| `-h, --help` | 打印帮助信息 |

### 示例

```bash
# 默认模式（d1_hit=2，两个碎片都击中 D1）
./extract_10C_4He -r 57 -e 70 -t main

# D1 仅 10C 模式（4He 在 D1 上丢失能量）
./extract_10C_4He -r 57 -e 70 -t main --d1_hit 1
```

### 输入文件

每个 run 需要读取以下文件（从 workspace 对应目录）：

| 文件 | 来源 | 内容 |
|------|------|------|
| `match/t0d1_<trigger>_<run>.root` | match_dssd | D1 正反面匹配事件 |
| `match/t0d2_<trigger>_<run>.root` | match_dssd | D2 正反面匹配事件 |
| `match/t0d3_<trigger>_<run>.root` | match_dssd | D3 正反面匹配事件 |
| `match/t0d4_<trigger>_<run>.root` | match_dssd | D4 正反面匹配事件 |
| `ingot/t0s_<trigger>_<run>.root` | 原始数据 | S1 CsI 事件 |
| `beam/beam_<trigger>_<run>.root` | sort_beam | 束流粒子分类 |
| `track/ppac_<trigger>_<run>.root` | track_ppac | PPAC 径迹 |
| `calibration/t0.txt` | calibrate_t0 | T0 能量刻度参数 |

### 输出文件

输出到 `workspace/10C+4He/` 目录（由 `config.toml` 中 `[paths]` 的 `10C_4He` 配置）：

```
extract_10C_4He_<trigger>_<run_start>_<run_end>.root       # d1_hit=2（默认）
extract_10C_4He_<trigger>_<run_start>_<run_end>_d1hit1.root # d1_hit=1
```

输出文件包含：
- **TTree `tree`**：C10He4Event 结构体，含能量、角度、束流分类、PPAC 径迹等全部信息
- **6 个 TH2D**：E1-E2、E2-E3 的 10C 和 4He 能量关联图，以及 4He 的 E3-E4、E4-E5 关联图

### 事件筛选流程

```
读取各探测器数据
    │
    ├── Pass10C_d3_4He_s1Cut 筛选：
    │   ├── d1.num == d1_hit（默认 2，可选 1）
    │   ├── d2.num == 2
    │   ├── d3.num == 2
    │   ├── d4.num == 1
    │   ├── 对 d1/d2/d3 的 hit 按能量排序
    │   ├── (E3_10C, E2_10C) 落在 cal_d2_d3_stop_10C_cut 内
    │   └── t0s.valid == true
    │
    ├── 碎片识别：能量大的 hit 为 10C，能量小的为 4He
    │   （d1_hit=1 时，d1 唯一 hit 即 10C，e1_4He=0）
    │
    ├── 能量刻度：CalibrateC10He4Energy(calib, layer, raw)
    │
    ├── 角度计算（依赖 PPAC 径迹）：
    │   ├── theta_beam, phi_beam：束流入射方向角
    │   ├── theta_10C：10C 相对于束流方向的散射角
    │   ├── theta_4He：4He 相对于束流方向的散射角
    │   └── opening_angle：10C 与 4He 之间的张角
    │
    └── 填充输出 TTree 和直方图
```

### d1_hit 参数说明

当 `d1_hit=1` 时，D1 上只有一个 hit，该 hit 被认定为 10C（4He 在 D1 上丢失能量）。此时：

- `e1_10C` = D1 上唯一 hit 的刻度能量
- `e1_4He` = 0
- 4He 总动能仅包含 e2 到 e5：T_4He = e2_4He + e3_4He + e4_4He + e5_4He
- 输出文件名添加 `_d1hit1` 后缀以区分

---

## GUI_10C_4He — 交互式可视化

### 运行方式

```bash
./GUI_10C_4He -c config.toml
```

启动后通过 `File → Open` 选择 `10C+4He/` 目录下的 `extract_10C_4He_*.root` 文件。

### 窗口布局

程序创建 3 个画布：

| 画布 | 标题 | 布局 | 说明 |
|------|------|------|------|
| 主界面（嵌入式） | `GUI_10C_4He` | 2×3 | 6 张原始能量关联直方图，展示全部事件 |
| 筛选画布（独立） | `10C+4He Filtered` | 2×3 | 经 cut 筛选 + 束流筛选后的 6 张直方图 |
| 分析画布（独立） | `10C+4He Analysis` | 2×2 | 物理分析图 |

### 主界面（嵌入式画布，2×3）

```
┌──────────────────┬──────────────────┬──────────────────┐
│  E1_10C-E2_10C   │  E2_10C-E3_10C   │  E1_4He-E2_4He   │
│  (E2_10C,E1_10C) │  (E3_10C,E2_10C) │  (E2_4He,E1_4He)  │
│  colz            │  colz            │  colz            │
├──────────────────┼──────────────────┼──────────────────┤
│  E2_4He-E3_4He   │  E3_4He-E4_4He   │  E4_4He-E5_4He   │
│  (E3_4He,E2_4He) │  (E4_4He,E3_4He) │  (E5_4He,E4_4He)  │
│  colz            │  colz            │  colz            │
└──────────────────┴──────────────────┴──────────────────┘
```

### 筛选画布（独立 TCanvas，2×3）

布局与主界面相同，但仅包含满足以下条件的事件：
1. `(e5_4He, e4_4He)` 落在 `cal_d4_s1_stop_4He_cut.C` 的 TCutG 区域内
2. 满足当前束流筛选条件

### 分析画布（独立 TCanvas，2×2）

```
┌─────────────────────────┬─────────────────────────┐
│  10C E-θ 关联            │  4He E-θ 关联            │
│  (θ_10C, T_10C)         │  (θ_4He, T_4He)         │
│  colz                   │  colz                   │
├─────────────────────────┼─────────────────────────┤
│  θ_10C vs θ_4He         │  14O 激发能谱            │
│  (θ_10C, θ_4He)          │  E_x (MeV)             │
│  colz                   │  (binning: 200, 5-25)   │
└─────────────────────────┴─────────────────────────┘
```

### 束流选择按钮

主窗口顶部提供 4 个束流选择按钮：

| 按钮 | 含义 | 画布 1 影响 | 画布 2、3 影响 |
|------|------|------------|---------------|
| `All` | 不过滤束流 | 无影响 | 显示全部筛选后事件 |
| `14O` | 仅 14O 束流 | 无影响 | 仅显示 is_14O=true 的事件 |
| `13N` | 仅 13N 束流 | 无影响 | 仅显示 is_13N=true 的事件 |
| `12C` | 仅 12C 束流 | 无影响 | 仅显示 is_12C=true 的事件 |

切换按钮时，画布 2 和画布 3 自动重填，直方图标题中会显示当前束流标签（如 `(14O)`）。

### Run 号范围选择

主窗口顶部提供两个 `TGNumberEntry` 控件，用于指定 run number 范围：

- **Run Min**：run 号下限（闭区间），默认 `0` 表示不设下限
- **Run Max**：run 号上限（闭区间），默认 `0` 表示不设上限

当两个值均为 `0`（默认）时，加载文件中所有事件；否则仅加载 `run_number` 在 `[min, max]` 区间内的事件。

该筛选在 `File → Open` 时生效，加载时即过滤，所有三个画布均受影响。控制台会打印筛选后的事件数与跳过的数量。

### Draw 按钮

`Draw` 按钮位于顶部控制栏最右侧。按下后，使用当前 run 范围和束流选择重新加载当前文件并重绘所有画布，无需重新打开文件对话框。

---

## 激发能计算原理

### 物理背景

14O 可以通过 α+10C 结构激发，其 α 分离阈值为 **E_break = 10.12 MeV**。当 14O 激发态衰变为 10C + α 时，通过测量两个碎片在实验室系中的四动量，可以利用不变质量法重建母核的激发能。

### 不变质量法（相对论四动量）

两个碎片的相对论四动量分别为：

- P_10C = (E_10C, **p**_10C)，其中 E_10C = T_10C + m_10C，|**p**_10C| = √(E_10C² − m_10C²)
- P_4He = (E_4He, **p**_4He)，其中 E_4He = T_4He + m_4He，|**p**_4He| = √(E_4He² − m_4He²)

两碎片系统的不变质量 M_inv 为：

```
M_inv² = (P_10C + P_4He)²
       = m_10C² + m_4He² + 2(E_10C·E_4He − p_10C·p_4He·cos(θ_open))
```

其中 θ_open 为 10C 与 4He 之间的张角（opening_angle）。

### 激发能计算

```
E_rel = M_inv − (m_10C + m_4He)          # 破裂时的相对能
E_x   = E_rel + E_break                  # 14O 激发能，E_break = 10.12 MeV
```

### 原子质量来源

通过 `brill::GetMass(6, 10)` 和 `brill::GetMass(2, 4)` 从 `assets/amdc_ion_2020.txt` 读取 10C 和 4He 的准确原子质量（单位：MeV/c²）。

### 代码实现

```cpp
double m_10C = brill::GetMass(6, 10);           // 10C 原子质量
double m_4He = brill::GetMass(2, 4);            // 4He 原子质量

double T_10C = ev.e1_10C + ev.e2_10C + ev.e3_10C;
double T_4He = ev.e1_4He + ev.e2_4He + ev.e3_4He + ev.e4_4He + ev.e5_4He;

double E_10C = T_10C + m_10C;                   // 10C 总能量
double E_4He = T_4He + m_4He;                   // 4He 总能量

double p_10C = std::sqrt(E_10C*E_10C - m_10C*m_10C);
double p_4He = std::sqrt(E_4He*E_4He - m_4He*m_4He);

double cos_open = std::cos(ev.opening_angle * M_PI / 180.0);

double M_inv_sq = m_10C*m_10C + m_4He*m_4He
    + 2.0 * (E_10C*E_4He - p_10C*p_4He*cos_open);
double M_inv = std::sqrt(M_inv_sq);

double E_rel = M_inv - (m_10C + m_4He);
double E_x = E_rel + kEbreak;                   // kEbreak = 10.12 MeV
```

---

## 依赖关系

```
extract_10C_4He
    ├── C10He4_event (10C_4He_event.cpp)
    ├── C10He4_extract (extract.cpp)
    ├── config
    ├── dssd_match_event
    ├── silicon_event
    ├── ppac_track_event
    ├── beam_sort
    ├── physics (kinematics.cpp)
    ├── rebuild_physics (nuclear_data.cpp)
    └── utils

GUI_10C_4He
    ├── C10He4_event
    ├── config
    ├── rebuild_physics (nuclear_data.cpp → GetMass)
    ├── physics (kinematics.cpp)
    └── utils
```

---

## 数据处理流程

```
ingot/ (原始数据)
    │
    ├── match_dssd ──────────→ match/t0d{1,2,3,4}_*.root
    ├── track_ppac ──────────→ track/ppac_*.root
    ├── sort_beam ───────────→ beam/beam_*.root
    ├── calibrate_t0 ────────→ calibration/t0.txt
    │
    └── extract_10C_4He ────→ 10C+4He/extract_10C_4He_*.root
                                  │
                                  └── GUI_10C_4He ──→ 可视化 + 激发能谱
```

---

## 配置文件

在 `config.toml` 的 `[paths]` 中添加：

```toml
[paths]
10C_4He = "10C+4He"
```

并在 workspace 下创建对应目录 `10C+4He/`。